name: workflow-1
on: push
jobs:
  jobs-1:
    runs-on: ubuntu-24.04
    steps:
      - name: checkout
        uses: actions/checkout@v7
      - name: install independencies
        run:  npm install
      - name: test
        run: npm test
      - name: starting the app
        run: echo "your app running on port 3000"
